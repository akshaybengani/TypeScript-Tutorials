// Here we declared variable a with value 5
var a = 5;
// Conditional statements if the statement is true then only the statements inside the if will execute
if (a > 0) {
    // This is to print
    console.log("a is positive");
}
